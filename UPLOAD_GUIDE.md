# رفع الكود إلى RP2040 عبر vREPL

## ✅ الطريقة الأسهل: النسخ المباشر

بما أن vREPL مفتوح بالفعل على COM6:

### الخطوات:

1. **في vREPL، اضغط `Ctrl+E`** لتفعيل وضع اللصق
   - سترى: `paste mode; Ctrl-C to cancel, Ctrl-D to finish`

2. **افتح ملف [`sensor_reader.py`](file:///f:/Nemr/COMPINE/compine/sensor_reader.py)**

3. **انسخ كل الكود** (`Ctrl+A` ثم `Ctrl+C`)

4. **الصق في vREPL** (`Ctrl+V` أو زر الماوس الأيمن)

5. **اضغط `Ctrl+D`** لتشغيل الكود

6. **ستبدأ البيانات بالظهور مباشرة!** 📊

---

## 🔄 لحفظ الكود بشكل دائم (يعمل عند إعادة التشغيل):

في vREPL:

```python
# 1. افتح ملف جديد للكتابة
f = open('main.py', 'w')

# 2. اكتب الكود (سنضيف الكود أدناه)
```

ثم استخدم **وضع اللصق** كما في الأعلى لكتابة الكود كاملاً إلى الملف.

---

## ⚠️ لماذا فشل ampy؟

**السبب:**
- vREPL يستخدم COM6 حالياً
- `ampy` يحتاج الوصول الحصري للمنفذ
- لا يمكن لبرنامجين فتح نفس المنفذ في نفس الوقت

**الحل إذا أردت استخدام ampy:**

1. **أغلق vREPL تماماً** (اخرج من MicroPython REPL)
2. في PowerShell (وليس داخل REPL):
   ```powershell
   ampy --port COM6 put sensor_reader.py main.py
   ```
3. أعد فتح vREPL لرؤية النتائج

---

## 🚀 الطريقة الموصى بها الآن:

**استخدم vREPL مباشرة بوضع اللصق `Ctrl+E`** - أسرع وأسهل! ✅
